'use client'

import { DashboardProps } from '@/utils/types'

const Dashboard = ({ user_id } : DashboardProps ) => {

    return (
        <div>
            This is your id {/* {user_id} */}
        </div>
    )
} 

export default Dashboard;